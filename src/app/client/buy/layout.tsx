import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Buy Facebook Number · SMSFlow",
  description: "Buy virtual Facebook numbers for OTP / SMS verification with live stock and PKR pricing.",
};

export default function BuyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
